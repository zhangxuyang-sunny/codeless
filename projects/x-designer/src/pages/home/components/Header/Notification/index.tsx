import { IconNotification } from "@arco-design/web-react/icon";
import HeaderButton from "../HeaderButton";

const Notification: React.FC = () => {
  return <HeaderButton icon={<IconNotification />} />;
};
export default Notification;
