import React from "react";
import styled from "styled-components";

const StyleCloudFunctionSettings = styled.div`
  min-width: 300px;
`;

const CloudFunctionSettings: React.FC = () => {
  return <StyleCloudFunctionSettings>逻辑编辑器</StyleCloudFunctionSettings>;
};

export default CloudFunctionSettings;
